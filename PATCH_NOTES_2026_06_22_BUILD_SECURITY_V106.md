# Patch v106 — Build, auditoria e pacote limpo

- Atualiza metadados internos do Proxy para `21.12.155-build-security-v106`.
- Corrige `scripts/audit-version.js` para validar o patch atual em vez do checkpoint antigo v57.
- Sincroniza `public/manifest.webmanifest` e `public/service-worker.js` para a versão pública `21.12.155`.
- Remove arquivo de backup `lib/analysis/analysis-page-response.js.bak` do pacote de entrega.

