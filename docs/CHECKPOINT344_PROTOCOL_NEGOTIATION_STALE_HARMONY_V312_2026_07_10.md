# Checkpoint 344 — Protocol negotiation and stale harmony v312

Pareado ao APK `2026.07.10.6` / v476.

## Alterações

- Protocolo móvel `2026.07.10.6` negociado por `X-Valorae-Mobile-Protocol`.
- Delivery schema v2 publicado por `X-Valorae-Delivery-Schema`.
- TTL `fast` 35 s e `full` 180 s.
- Stale grace `fast` 120 s e `full` 900 s.
- Cache da Análise limitado a 60 s.
- Respostas JSON/texto e camada de performance usam os mesmos metadados.

## Validação

- Build e sintaxe.
- 187 arquivos de teste, zero falhas.
- Auditoria de versão.
- Execução isolada a partir do ZIP final.
